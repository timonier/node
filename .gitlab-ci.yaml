stages:
    - deploy

# ---

deploy:image:
    before_script:
        - 'apk add --no-cache --no-progress ca-certificates curl'
    image:
        entrypoint: ['']
        name: alpine:latest
    only:
        - master
    script:
        - 'curl --request POST "https://cloud.docker.com/api/build/v1/source/f1c340df-24a8-4d63-9f3c-d3b91d3e10f9/trigger/${DOCKER_CLOUD_TOKEN}/call/"'
    stage: deploy
